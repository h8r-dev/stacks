apiVersion: v1
kind: Service
metadata:
  name: {{ include "gin-next.fullname" . }}
  labels:
    {{- include "gin-next.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "gin-next.selectorLabels" . | nindent 4 }}
