apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gin-next.fullname" . }}-test-connection"
  labels:
    {{- include "gin-next.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gin-next.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
